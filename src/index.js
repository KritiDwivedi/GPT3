import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css'
// This line means that we are going to render our application(App) 
// inside the div element having an id of root
ReactDOM.render(<App/>,document.getElementById('root'));